#!/usr/bin/python3
"""This module contain lookup module."""


def lookup(obj):
    """function that returns the list of available
            attributes and methods of an object."""
    return (dir(obj))
